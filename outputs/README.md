# Outputs Directory

Use this folder for generated artifacts created during analysis.

- `figures/`: plots, charts, and visual exports
- `tables/`: result tables or intermediate exports

These folders are ignored by Git except for placeholder files, so you can save local outputs here without polluting the repository history.
