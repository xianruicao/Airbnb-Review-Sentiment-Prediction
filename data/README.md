# Data Directory

Use this folder for local datasets that should not be committed to Git.

- `raw/`: original downloaded files such as `reviews.csv`
- `processed/`: cleaned or transformed local datasets

The current notebook can run from externally hosted CSV files, so these folders are mainly here to keep the repository organized if you work with local data.
